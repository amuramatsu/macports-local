# -*- coding: utf-8; mode: tcl; tab-width: 4; indent-tabs-mode: nil; c-basic-offset: 4 -*- vim:fenc=utf-8:ft=tcl:et:sw=4:ts=4:sts=4

PortSystem          1.0

name                texlive-bin
version             0
revision            0

categories          tex
maintainers         @amuramatsu
license             Copyleft Permissive
description     (DUMMY) TeX Live Binaries.
long_description    (DUMMY) TeX Live is an easy way to get up and running with TeX. \
    It provides a comprehensive TeX system with binaries for most flavors \
    of Unix, including GNU/Linux, and also Windows. It includes all the \
    major TeX-related programs, macro packages, and fonts that are free \
    software, including support for many languages around the world.

use_configure no
fetch {}
checksum {}
extract {}
build {}

destroot {
    file mkdir -p ${destroot}${prefix}/share/doc/${name}
    touch ${destroot}${prefix}/share/doc/${name}/DUMMY
}
